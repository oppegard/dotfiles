#!/bin/bash
set -euxo pipefail

cd $HOME/workspace/wf/wavefront-proxy/proxy

JAR=target/proxy-11.0-RC3-SNAPSHOT-uber.jar
[[ -e $JAR ]] || mvn install -DskipTests -Djacoco.skip=true

lsof -i tcp:2878|awk '/LISTEN/ {print $2}'|xargs kill || true
java -jar $JAR -f $HOME/.config/wf/proxy.ini 2>&1
